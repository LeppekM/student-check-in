package Tables;

/**
 * Enum for TSCTable's inheritors / what mode TableScreenController is in
 */
public enum TableScreen {
    COMPLETE_INVENTORY,
    HISTORY,
    CHECKED_OUT,
    OVERDUE,
    WORKERS,
    STUDENTS
}
